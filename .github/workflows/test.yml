name: Test Features

on:
  push:
    branches:
      - main
    paths:
      - "src/**"
      - "test/**"
      - "scripts/**"
      - ".github/workflows/test.yml"
  pull_request:
    paths:
      - "src/**"
      - "test/**"
      - "scripts/**"
      - ".github/workflows/test.yml"
  workflow_dispatch:

jobs:
  test-autogenerated:
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      matrix:
        feature:
          - websocat
          - mcp-grafana
          - migrate
          - promtool
        base-image:
          - debian:bookworm
          - ubuntu:22.04
          - mcr.microsoft.com/devcontainers/base:ubuntu
    steps:
      - uses: actions/checkout@v4

      - name: Prepare bundled Feature assets
        run: scripts/prepare-feature-assets.sh ${{ matrix.feature }}

      - name: Install @devcontainers/cli
        run: npm install -g @devcontainers/cli

      - name: "Test autogenerated: ${{ matrix.feature }} on ${{ matrix.base-image }}"
        run: |
          devcontainer features test \
            --skip-scenarios \
            -f ${{ matrix.feature }} \
            -i ${{ matrix.base-image }} \
            .

  test-scenarios:
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      matrix:
        feature:
          - websocat
    steps:
      - uses: actions/checkout@v4

      - name: Install @devcontainers/cli
        run: npm install -g @devcontainers/cli

      - name: "Test scenarios: ${{ matrix.feature }}"
        run: |
          devcontainer features test \
            --skip-autogenerated \
            -f ${{ matrix.feature }} \
            .
